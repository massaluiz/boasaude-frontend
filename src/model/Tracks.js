export default class Track {


    constructor(id, 
        action, 
        user, 
        createAt) {

        this.id = id;
        this.action = action;
        this.user = user;
        this.createAt = createAt;
    }
}